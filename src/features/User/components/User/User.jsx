import React from 'react';
import PropTypes from 'prop-types';

User.propTypes = {

};

function User(props) {
    return (
        <div>
            User
        </div>
    );
}

export default User;